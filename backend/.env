NODE_ENV=development
PORT=4000

DB_HOST=mysql-5d74bc8-adarshshukla2608-a333.f.aivencloud.com
DB_PORT=21891
DB_NAME=defaultdb
DB_USER=avnadmin
DB_PASSWORD=AVNS_cKY_G9uueTPGOLoXTFq
DB_SSL=true

JWT_SECRET=shdbsnskjsj33876673648343%%%******0wjdjwd7ybsdcbdsh
JWT_EXPIRES_IN=1d

CLIENT_URL=http://localhost:3001

# --- Email (SMTP) – used by IQLead to send mail (per-user credentials set in app) ---
# Server-level: host and port. User passwords are stored encrypted per user in DB.
# Use ONE option below: uncomment that block and comment the other.

# --- Gmail ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# --- Hostinger (uncomment to use; comment Gmail block above) ---
 #SMTP_HOST=smtp.hostinger.com
 #SMTP_PORT=465

# Encryption key for storing user SMTP passwords in DB (required for Settings → Integrations / Users → SMTP).
# Generate once: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SMTP_CRED_KEY=yYqFzYHZW++/uzezxAq9qV/HWpjWERwg8s74lqmhgGA=

# --- Web Push (desktop/mobile browser push notifications) ---
# Generate VAPID keys and set these to enable push delivery.
# Public key is safe to expose; private key must be kept secret.
VAPID_PUBLIC_KEY=BFiqYpaLD5GLry8qKyHVKsP8voEOdz32OEKz_bV10t7piBMja5flmtI2IwM3_KUI2cnWtsBFIYzB6T6WgLfea_o
VAPID_PRIVATE_KEY=Acwfp-ehxp-qN4My2WNrQnrKD313w_Aa1_T2aFGwjsI
# Example: mailto:admin@yourdomain.com
VAPID_SUBJECT=https://iqlead.com
